# Only scripts/dev.mjs reads this file. Never put these values in frontend configuration.
GEMINI_API_KEY=
CONVERSATION_SECRET=
# Public URL used when building GitHub Pages; empty means same-origin /api.
SAANTAYO_API_BASE=

# Optional server-only structured journey routing
GOOGLE_ROUTES_API_KEY=
# Optional reviewed regional estimate models; see docs/journey-planner.md
GRAB_ESTIMATE_CALIBRATIONS=[]
